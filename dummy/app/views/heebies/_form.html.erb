<%= form_for(heeby) do |f| %>
  <% if heeby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(heeby.errors.count, "error") %> prohibited this heeby from being saved:</h2>

      <ul>
      <% heeby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.text_field :age %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
